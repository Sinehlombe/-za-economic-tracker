# -za-economic-tracker

A production-style ETL pipeline that extracts South African economic indicators from the World Bank API, transforms and validates the data, loads it into PostgreSQL, and surfaces it through a FastAPI + React dashboard.
